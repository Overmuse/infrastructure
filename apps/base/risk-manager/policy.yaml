---
apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: risk-manager
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: risk-manager
  policy:
    semver:
      range: 0.4.x

